initVar oneBefore = 0;
initVar twoBefore = 0;
initVar current = 0;

forEach {
    function (fVar x) {
        updateVar current = f$x + $oneBefore + $twoBefore;
        updateVar twoBefore = $oneBefore;
        updateVar oneBefore = $current;
        return ($current)
    }
};